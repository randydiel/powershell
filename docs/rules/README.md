# Alliance rules

> **Approved by:** `<rank/handle>` — `<YYYY-MM-DD>`

## The short list

The rules that actually get you removed, stated plainly:

1. `<rule>`
2. `<rule>`
3. `<rule>`

Everything below is detail on these.

## Conduct

- `<expected behaviour in alliance chat>`
- `<what is not tolerated>`

## Activity requirement

| Requirement | Standard | Grace |
| --- | --- | --- |
| Daily login | `<>` | `<>` |
| Event participation | `<>` | `<>` |
| Inactivity notice | `<>` | `<>` |

State the notice process: where to post, how far ahead, how long a hold
lasts before the slot is reclaimed.

## Participation

- **Rallies:** `<join expectations>`
- **Reinforcement:** `<expectations>`
- **Resource sharing:** `<expectations>`

## Enforcement

| Step | Who acts | What happens |
| --- | --- | --- |
| First issue | `<rank>` | `<warning process>` |
| Repeat | `<rank>` | `<consequence>` |
| Removal | `<rank>` | `<who decides, appeal route>` |

An appeal route matters — see [governance](../governance/README.md).
