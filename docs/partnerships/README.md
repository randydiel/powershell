# Partnerships and diplomacy

> **Approved by:** `<rank/handle>` — `<YYYY-MM-DD>`
> **Last reviewed:** `<YYYY-MM-DD>`

<!-- markdownlint-disable-next-line MD028 -->

> [!CAUTION]
> Check this table before attacking anyone. Hitting an ally or a NAP'd
> alliance is a diplomatic incident, and "I didn't check" is not a defence.
> If the table looks out of date, ask `<rank/handle>` — do not assume.

## Standing

| Alliance | Tag | Standing | Since | Terms | Their contact |
| --- | --- | --- | --- | --- | --- |
| `<name>` | `<TAG>` | Ally | `<date>` | `<link/summary>` | `<handle>` |
| `<name>` | `<TAG>` | NAP | `<date>` | `<link/summary>` | `<handle>` |
| `<name>` | `<TAG>` | Neutral | `<date>` | — | `<handle>` |
| `<name>` | `<TAG>` | Hostile | `<date>` | — | — |

### Standing definitions

- **Ally** — mutual defence; reinforce on request.
- **NAP** — non-aggression pact; do not attack, no defence obligation.
- **Neutral** — no agreement; standard rules of engagement apply.
- **Hostile** — active conflict; see current orders from `<rank>`.

Adjust these definitions to match what was actually agreed — the labels
only help if everyone reads them the same way.

## Rules of engagement

- **Before attacking:** `<check process>`
- **If you hit someone by mistake:** `<what to do, who to tell>`
- **If we're attacked by an ally/NAP:** `<escalation path — do not retaliate
  unilaterally>`

## Changing a standing

Diplomacy changes are a leadership decision — see the decision table in
[governance](../governance/README.md). When a standing changes:

1. Update the row and the **Last reviewed** date above.
2. Announce in `<channel>` — members will not re-read this page on their own.
3. Note the change in the log below.

## Change log

| Date | Alliance | Change | Decided by |
| --- | --- | --- | --- |
| `<>` | `<>` | `<>` | `<>` |
