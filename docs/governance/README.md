# Governance and ranks

> **Approved by:** `<rank/handle>` — `<YYYY-MM-DD>`

## Ranks at a glance

Use the in-game rank names so the table matches what members actually see.

| Rank | Who holds it | Can do | Cannot do |
| --- | --- | --- | --- |
| `<rank>` | `<handles>` | `<powers>` | `<limits>` |
| `<rank>` | `<handles>` | `<powers>` | `<limits>` |
| `<rank>` | `<handles>` | `<powers>` | `<limits>` |

## Promotion

| From → To | Criteria | Who decides |
| --- | --- | --- |
| `<>` → `<>` | `<>` | `<>` |
| `<>` → `<>` | `<>` | `<>` |

Write criteria as observable facts (weeks active, events attended), not
vibes. Members should be able to tell whether they qualify without asking.

## How decisions get made

| Decision type | Who decides | Consulted first |
| --- | --- | --- |
| Day-to-day (rallies, events) | `<rank>` | — |
| Rule changes | `<rank>` | `<who>` |
| Diplomacy (new ally, NAP, war) | `<rank>` | `<who>` |
| Removals | `<rank>` | `<who>` |

## Disputes and appeals

1. **Raise it** with `<rank>` in `<channel>` — not in main alliance chat.
2. **Review** by `<rank>` within `<timeframe>`.
3. **Final call** by `<rank>`.

Say explicitly what a member can expect: who hears it, how long it takes,
and whether the outcome is final.

## Changing this document

Policy pages change by leadership decision, not by edit. The process:

1. Proposal raised with `<rank>`.
2. Decision recorded per the decision table above.
3. Page updated, `Approved by` line restamped with the new date.
